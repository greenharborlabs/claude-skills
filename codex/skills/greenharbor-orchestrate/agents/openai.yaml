interface:
  display_name: "Orchestrate"
  short_description: "Execute plan files with review-fix cycles"
  default_prompt: "Use $greenharbor-orchestrate to execute this plan file"

policy:
  allow_implicit_invocation: true
