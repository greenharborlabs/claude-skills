interface:
  display_name: "Next Phases"
  short_description: "Audit a repo and produce a roadmap"
  default_prompt: "Use $greenharbor-next-phases to audit this repository"

policy:
  allow_implicit_invocation: true
